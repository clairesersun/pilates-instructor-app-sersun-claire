/*
To do:
only show this page if the user is logged in. If they are not, redirect them to the login page.
-- take the exercise database and filter the information as props throughout this page
-- add a back button on top lefthand corner
<h1>{name}</h1>
-- the add button component will pop up when it is clicked. when the click outside of the component or finish a task it mides the module.
<section>
    <section>
        <p>{planeofmotion}</p>
        <p>{spinalmovements}</p>
        <p>{bodyclassification}</p>
        <p>{equipment}</p>
        <p>{history}</p>
    </section>
    <section>
        <h2>Description</h2>
        <p>{description}</p>
        <p>{image}</p>
        <p>{media}</p>
        <p>{variations}</p>
    </section>
</section>
<Link href="exercise/addexercise" />
*/
